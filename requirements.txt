# gfdash 依存パッケージ
#
# ★ 全て固定しています。固定しないと、コードを1行も変えていなくても
#   再ビルドするだけで構成が変わり、障害が起きたときに「自分の変更が原因」なのか
#   「依存が入れ替わったせい」なのかを切り分けられなくなります。
#   実際 2026-08-26 の再ビルドで pandas が 2系 -> 3.0.5、numpy が -> 2.5.2 に
#   上がりました。
#
# 動作確認: Python 3.13 / linux x86_64 (2026-08-26)
#
# 更新するとき:
#   docker compose exec web pip install -U <パッケージ名>
#   docker compose exec -T web pip freeze        # 結果を下記に反映(このコメントは残す)
#   → 反映後、必ず画面を開いて動作を確認してください

# --- 直接の依存 ---
asgiref==3.9.1
Django==6.0
sqlparse==0.5.0
typing_extensions==4.7.1
# psycopg2 は psycopg2-binary と同じ psycopg2 モジュールを提供します。
# 両方入れると psycopg2 のソースビルドだけでイメージ作成が15分近くかかるため、
# ビルド済み wheel の psycopg2-binary のみを使います(2026-08-26)。
# 自前ビルド版に切り替える場合は、下の行を psycopg2==2.9.12 に変え、
# Dockerfile に libpq-dev / gcc の導入を追加してください。
psycopg2-binary==2.9.12
django-bootstrap5==26.2
python-dateutil==2.9.0.post0
pandas==3.0.5
requests==2.34.2
lxml==6.1.2
html5lib==1.1
beautifulsoup4==4.15.0
whitenoise==6.12.0
gunicorn==26.2.0
prophet==1.4.0

# --- 間接の依存(上記が引き込むもの。再現性のため固定) ---
certifi==2026.7.22
charset-normalizer==3.5.1
cmdstanpy==1.3.0
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
holidays==0.103
idna==3.19
kiwisolver==1.5.0
matplotlib==3.11.1
numpy==2.5.2
packaging==26.3
pillow==12.3.0
pyparsing==3.3.2
six==1.17.0
soupsieve==2.9.2
stanio==0.5.1
tqdm==4.70.0
urllib3==2.7.0
webencodings==0.6.1

#dockerではなくローカルで実行する場合は、以下のライブラリをインストールして下さい。
#python-dotenv